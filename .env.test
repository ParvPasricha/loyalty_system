# Shared test defaults (safe to commit)
# Intended for local/integration testing with docker compose services.

NODE_ENV=test
PORT=3001
JWT_SECRET=test-secret
DATABASE_URL=postgres://loyalty:loyalty@localhost:5432/loyalty
NEXT_PUBLIC_API_BASE=http://localhost:3001
STAFF_ROLE=cashier
